# 회원 배정 도우미 (배정 영어 6-8)

`index.html` 단일 파일 HTML/JS 도구. SheetJS(XLSX)로 두 엑셀 파일을 브라우저에서 직접 읽어
배정 필요 회원과 교사 스케줄을 매칭한다.

## 입력 파일
1. **배정 파일**: `★배정` 시트. 행 9(0-index 8)부터 회원 데이터.
2. **교사 스케줄러 파일**: 교사별 탭(스케줄러). 탭 이름에서 교사명/레벨 추출 (`parseTabName`).

## 모드 (`currentMode`)
- `eng`: 그룹영어 (레벨 기반 매칭, ENG_TEACHER/ENG_DATE 컬럼 사용). 예전에는 "영어 자주표"(`engJj`, AX열 필터)가 별도 옵션으로 있었으나 사용자 요청으로 `eng` 하나로 통합함(select 옵션 제거, AX열 필터 블록 삭제). 코드 내부에는 여전히 `currentMode === 'eng' || currentMode === 'engJj'` 형태의 체크가 남아있는 곳이 있는데, `engJj` 값이 더 이상 설정되지 않으므로 무해한 죽은 조건이다(제거하지 않고 남겨둠).
- `gen`: 그룹일반 (MGR_TEACHER/MGR_DATE 컬럼 사용, 학년 기반)
- `genN`: 그룹일반(N) - 관리교사 매주/격주(N) 혼합 배정 (gen과 입력 컬럼은 동일, 매칭/용량 로직만 다름)

## 컬럼 매핑 (`COL`, 0-indexed)
- `POSSIBLE_DAYS` (AJ, 35): 가능일정(수기) — 불가능 시간 설문을 사람이 보기 좋게 옮긴 것
- `AP_DAYS` (AP, 41) ~ `AW_REQUEST` (AW, 48): **불가능시간(설문 원문)**
  - AP: 요일 관련 — "가능" 문구 없이 요일만 있으면 그 요일 불가, "○○ 가능" 식 문구 있으면 가능 정보로 그대로 사용 (`getApAnalysis`). AJ에 요일 정보가 없을 때만 가능요일 판단에 사용됨 (`parseAvailability`)
  - AQ~AV: 시간대별 불가 체크(X). 화면에는 "가능 시간대"로 변환해서 표시 (`getAvailableTimesDisplay`)
  - AW: 요청사항 원문, 그대로 표시
- `AX_ENG` (49): engJj 모드 필터용 ("영어" 포함 여부)
- `FIRST_WEEK` (17): 첫 수업 주간. 미입력 회원은 미로그인 상태로 간주하여 제외 (화면/엑셀에서 "수업주차"로 표시, `firstWeekCell`)
- `BRO_CAS`/`BRO_2CAS`/`BRO_NAME`/`BRO_MGR`/`BRO_ENG`: 형제회원 정보

## 핵심 로직
- `parseAvailability(apRaw, ajRaw)`: 가능요일 판단. **AJ(가능일정 수기) 우선, 없으면 AP(불가능시간 설문 답변) 보조.**
  - AJ에 요일 정보가 있으면 그대로 가능요일로 사용 (AP는 무시).
  - AJ에 요일 관련 내용이 전혀 없을 때만 AP 기준으로 판단(`getApAnalysis`): "불가능"/"불가" 제거 후 "가능" 포함 여부로 분기.
  - 근거: 설문 답변을 정리하거나 전달받은 특이사항을 AJ에 옮겨 적기 때문에, 둘 다 있으면 AJ가 최신본. AJ가 비어있는데 AP만 있으면 AJ 작성 전에 설문만 인입된 상태.
  - `apDisplay`(불가능시간(설문 답변) 표시용 "요일: ..." 문구)는 AJ 우선 여부와 무관하게 항상 AP 분석 결과(`getApAnalysis`)를 사용.
- `dayRestricted` 플래그: `possibleDays`가 빈 배열일 때 "정보 없음(전체 가능)"과 "요일 전체 불가"를 구분.
- `isVideoRefused`: "화상코칭거부" 회원은 시간 무시하고 레벨만으로 매칭.
- `getAvailableTimesDisplay`: AQ~AV(불가 체크)을 사람이 읽기 좋은 "가능 시간대" 문자열로 변환.
- `TIME_SLOTS`: `3:00` ~ `8:30` (8:30~9:00이 마지막 타임, 9~10시 없음).
- `matchAll`: 회원-교사 매칭. 레벨 일치 + 요일/시간 가능 + 8명 미만 슬롯.
- `randomAssign`: 0단계로 "마감임박(7/8)" 매칭이 있는 회원을 우선 배정(`hasExactFitMatch`/`isExactFitSlot`)한 뒤, 나머지를 라운드로빈 방식으로 교사당 최대 N명 배정 (인원 부족 시 고루 분배). 우선 배정된 회원은 `_exactFitPicked = true`로 표시되어 화면에 🔶마감임박 배지로 표시됨. 결과는 `lastRandomAssign`에 저장되어 엑셀 다운로드에도 반영됨.
- **마감임박(7/8) 강조**: `EXACT_FIT_COUNT = 7`. `currentCount >= 7`인 슬롯(이 회원 배정 시 8/8 마감 완성)은 화면에서 `.slot-item.warn`/`badge-yellow`로 표시되고, 엑셀 다운로드에서도 `XLSX_YELLOW_FILL`(FFF59D)로 동일하게 강조됨 (배정필요회원전체의 추천교사 셀, 임의배정결과의 마감임박 행 전체, 교사별 탭의 가능시간(현재인원) 셀).

## 그룹영어(eng/engJj) 전용 규칙

- **예비초/파닉스/알파벳 레벨 처리**: Z열(`COL.LEVEL`) 값이 "예비초" 또는 "알파벳"이면 매칭용 `member.level`을 "파닉스"로 정규화(파닉스 반에 통합 배정). "파닉스"는 그대로 유지. (`parseAssignmentFile` 내 isEng 분기)
- **관리교사 겸함 안내**: H열(`COL.GRADE`) 값에 "초등6"/"6학년"/"예비초"가 포함되면 `member.engIsAlsoMgr = true`. 이 회원은 영어교사가 관리교사를 겸하므로, 화면(회원표 배지)과 엑셀(배정필요회원전체·교사별 탭의 "관리교사겸함" 컬럼)에 안내만 표시함 — 관리교사(gen/genN) 매칭 로직 자체는 건드리지 않음(이번 작업 범위는 eng/engJj로 한정).
- **자주표(자기주도표) A주/B주 격주 배정**: B열(`COL.GUBUN`, 구분)에 "자주표"가 포함되거나, 상품명(I열)이 "...캐츠홈(자기주도학습표)" 패턴에 해당하는 회원(`member.isJaju`, `productCategory()`와 `isJajuMember()` 둘 중 하나만 맞아도 인정 — UI 6차 개편에서 상품명 기준 추가, 아래 참고)은 격주 코칭.
  - **A/B주 판별**: R열(`COL.FIRST_WEEK`, 첫 수업주간, 항상 월요일 날짜)을 기준일(`AB_WEEK_ANCHOR` = 2026-08-24, B주)로부터 2주 간격 패턴으로 계산 (`getAbWeek`). 8/24 기준일 자체가 B주, 8/31이 A주이며 이후(과거 포함) 2주마다 반복.
  - **이미 배정된 회원의 A/B 조회**: 교사 스케줄러 파일에는 회원의 구분/첫수업주간 정보가 없으므로, ★배정 시트의 **전체 행**(배정 여부 무관)을 CAS로 색인한 `fullMemberIndex`(`buildFullMemberIndex`)로 역조회해 자주표 여부·A/B주를 판별함. `processFiles()`에서 `parseSchedulerFile` 호출 전에 반드시 먼저 빌드해야 함.
  - **용량 모델**: `parseSchedulerFile`의 eng/engJj 분기에서 슬롯별로 `weekly`(전과목·단과 등 매주 회원)/`jajuA`/`jajuB` 인원을 따로 집계 → `countA = weekly+jajuA`, `countB = weekly+jajuB`.
    - 매주(비자주표) 신규 회원: `weekly+1+jajuA <= 8 AND weekly+1+jajuB <= 8`인 슬롯만 가능(두 주 모두 참여하므로).
    - 자주표 신규 회원: 트랙(A/B)당 최대 2명(`jajuX < 2`) + 그 주 전체 정원 8명(`weekly+jajuX < 8`)을 모두 만족해야 함. 첫수업주간 기준 자기 트랙을 우선 시도하고, 그 트랙이 꽉 찼으면(2명) 반대 트랙으로 배정(=1주 늦게 시작, `matchAll`의 eng/engJj 분기).
  - **화면/엑셀 표시**: `jajuCycleLabel`/`cycleCategoryText`/`cycleCategoryTextWithWeek` 헬퍼로 genN의 "코칭주기/배정주차"와 동일한 위치(회원표·교사패널·임의배정·엑셀 각 시트)에 "코칭구분"(자주표/전과목·단과) 및 배정주차(A주/B주) 표시. (교사패널의 구 "가능 시간대 보기" A/B 배지 표는 이후 캘린더 모달로 대체되어 삭제됨 — 아래 UI 개편 섹션 참고)
  - **범위**: 이 로직 전체는 `eng`/`engJj` 모드에서만 동작 (`isEngJajuMode()`). `gen`/`genN`은 기존 로직 그대로 유지, 영향 없음(회귀 테스트 완료).
  - ⚠️ 실제 데이터 파일로 검증되지 않음 — Node 기반 모의(mock) 데이터로 로직 단위 테스트만 완료함 (아래 미해결 항목 참고).

## genN 모드 (그룹일반(N) - 매주/격주 혼합)
- **격주(N) 회원 판별**: `COL.PRODUCT`(I열, 현재 이용중 구독상품)에 `(N)`이 포함되면 격주코칭 회원 (`member.isBiweekly = true`). 예: "캐츠잉글리시+캐츠홈 2년(N)". 특이사항(AK)의 "전과목 격주코칭" 메모는 일부 행에만 있어 신뢰 불가 → 사용 안 함.
- **교사 스케줄러 파싱 (`parseTeacherTabGenN`)**: CN1형 탭(`5-11 권다원(1,2,4)` 등) 전용. 데이터는 행9(aoa idx 8)부터, 컬럼은 C=회원번호(idx2) D=회원명(idx3) E=학년(idx4) G=요일(idx6) H=시간(idx7) M=주차구분 'A주'/'B주'(idx12) N=매주 boolean(idx13).
  - 매주 회원 → 해당 슬롯의 `slotA`/`slotB` 둘 다 +1
  - 격주(A주) 회원 → `slotA`만 +1 / 격주(B주) 회원 → `slotB`만 +1
  - 주차구분 정보 없음 → 보수적으로 매주 취급(양쪽 다 +1)
- **용량 모델 (countA/countB)**: 슬롯별로 A주 인원수와 B주 인원수를 따로 집계.
  - 매주 신규 회원: `countA < 8 AND countB < 8`인 슬롯만 가능
  - 격주(N) 신규 회원: `countA < 8 OR countB < 8`인 슬롯 가능, 자리가 더 많이 남은 주(countA/countB가 더 적은 쪽)에 배정 (`assignedWeek`), 동률이면 A주 우선
- **화면/엑셀 표시**: 회원표·매칭결과·임의배정·교사패널·엑셀 모든 시트에 "코칭주기"(매주/격주(N)) 및 "배정주차"(A주/B주) 컬럼·배지 추가. 교사패널 시간대 표는 A x/8, B x/8 두 배지로 표시.

## UI 개편 (요약 카드 / 회원별 보기 / 임의 배정 / 캘린더 모달)

- **요약 카드**: `.stat-chip`을 전부 노란색 계열로 통일(이전엔 칩마다 파랑/초록/노랑이 섞여 있었음). "매칭 어려움" 칩만 `.stat-chip.danger`(빨간 굵은 테두리)로 강조하고 클릭 가능하게 만듦 — 클릭 시 `toggleNoMatchList()`가 매칭 안 된 회원의 CAS/이름/레벨 목록(`#noMatchList`)을 펼침/접음. "전체 가능 슬롯" 칩은 삭제.
- **회원별 보기 테이블(`renderMemberTable`) 재구성**:
  - 컬럼 순서: # · 첫수업주간 · 상품 · CAS번호 · 레벨 · 학년 · 매칭 가능 교사 및 시간 · 가능 일정(수기_AJ열) · 불가능시간(설문 답변) · 특이사항 · 형제. (사용자가 지정한 "상품/CAS/레벨/학년/매칭교사시간/가능일정/불가능일정/특이사항/형제" 순서 그대로 두고, 순번·첫수업주간만 맨 앞에 유지)
  - "상품" 컬럼 신설(`productCell(m)` 원본 상품명) — 기존에 "레벨" 칸에 같이 표시되던 코칭구분 배지(자주표/전과목·단과)와 관리교사겸함 배지를 이쪽으로 옮김. "레벨" 칸은 이제 레벨 배지만 표시.
  - "학년" 표시는 `gradeDisplay(m.grade)`로 "초등" 글자를 제거하고 숫자(또는 "예비초" 등)만 표시.
  - 날짜 포맷: `fmtDate`를 `YY-MM-DD`(예: `26-08-24`)로 변경 — 이 함수를 쓰는 다른 화면(수업주차 등)에도 동일하게 적용됨.
  - 정렬: `matchedTeacherCount(r)`(hasSlots인 매칭 교사 수) 오름차순으로 정렬 — 매칭 안 된 회원(0명)이 자동으로 맨 위에 오고, 그다음 매칭 가능 교사가 적은 순으로 나열됨. `Array.sort`가 stable이라 동점자는 기존 순서(첫수업주간 빠른 순)가 유지됨.
- **교사별 임의 배정 인원**: 기존 단일 `nPerTeacher` 입력을 제거하고, `renderPerTeacherTargets()`가 처리 완료 후 `teachersData` 기준으로 교사별 인원 입력 칩(`#perTeacherTargets`, id=`pt_<safeTeacherId>`)을 렌더링. "전체 일괄 적용" 버튼(`applyBulkPerTeacher`)으로 한번에 채울 수 있음. `randomAssign()`은 `getPerTeacherTarget(tabName)`으로 교사별 목표치를 읽어(빈 값=무제한) 배정 — 라운드로빈/마감임박 우선 배정 로직 자체는 동일, 비교 기준만 전역 n → per-teacher `targets[tabName]`로 변경.
- **가능 시간대 캘린더 모달**: 임의배정결과·교사별 보기의 "교사 가능시간" 칸이 텍스트 나열 대신 `slotModalButton()` 버튼("📅 시간표 보기")으로 바뀜. 클릭하면 `openSlotModal(cas, tabName)` → `buildSlotGridHtml(teacher, mt)`가 요일×시간 캘린더를 그려서 모달(`#slotModalOverlay`)에 표시:
  - 이 회원이 실제로 들어갈 수 있는 칸(`mt.availableSlots`)은 빨간 테두리 + `matchSlotLabel()`("Lv2(A) 1/8" 형식, A/B 트랙 없는 슬롯은 "Lv2 1/8")로 표시.
  - 그중 요일순→시간순으로 가장 이른 칸에는 `findEarliestSlotKey()`가 찾은 위치에 ⭐(가장 추천) 표시.
  - 나머지(회원 기준으로는 안 열려있는) 칸은 회색으로 교사 방의 현재 인원만 참고용으로 표시(`teacherSlotLabel()`).
  - 교사별 보기(`renderTeacherPanels`)의 구 "가능 시간대 보기" `<details>` 그리드(전체 A x/8, B x/8 배지 표)는 이 모달로 대체되어 완전히 삭제됨. "배정 가능 회원" 목록은 유지.
- **엑셀 다운로드는 변경 없음** — 위 모달/정렬/칩 변경은 화면(웹 UI) 전용이며, `downloadExcel()`의 시트 구성·컬럼은 이전과 동일(모달 대신 텍스트로 시간 목록 표시).
- **병합 셀 대응(`fillMergedCells`)**: 교사 스케줄러 파일이 요일/시간 등을 세로로 병합한 "캘린더" 형태일 경우, SheetJS `sheet_to_json(header:1)`은 병합 범위의 맨 왼쪽/맨 위 셀에만 값을 채우고 나머지는 `null`로 반환한다 — 이로 인해 병합된 행의 회원이 통째로 누락(요일/시간 없음 처리)될 수 있음. `ws['!merges']`를 읽어 병합 범위 내 모든 셀에 값을 채워 넣는 `fillMergedCells(ws, aoa)`를 `parseSchedulerFile`(genN 포함)과 `parseAssignmentFile`/`buildFullMemberIndex` 모두에 적용함. "교사별 가능 시간대가 실제 스케줄러 내용과 다르게 보인다"는 문제의 유력한 원인으로 보고 수정했으나, **실제 파일로 최종 확인 필요**(아래 미해결 항목).

## UI 2차 개편 (교사별 임의배정 인원 사전 설정 / 회원별 보기 재구성 / 슬롯 레벨 게이트 / 임의배정 결과 재구성)

- **교사별 임의 배정 인원 설정을 항상 노출**: 카드를 `#resultsArea`(비교 시작 전 `display:none`) 밖, 파일 업로드 카드 바로 아래(②)로 이동. 비교 시작 전에도 "전체 일괄 적용" 입력은 항상 동작. `extractTeacherNamesFromWb(wb)`가 스케줄러 파일(`wb2`)의 탭 이름만 가볍게 읽어(`parseTabName`, 전체 파싱 없이) `previewTeacherList`를 만들고, `[🔍 교사별 회원 수 설정]` 버튼(`loadTeacherNamesForTargets()`)으로 비교 시작 전에 교사별 인원 입력칸을 미리 채울 수 있음. `renderPerTeacherTargets(list)`는 `list` 생략 시 `teachersData.length ? teachersData : previewTeacherList`를 사용하므로, 비교 시작 후에는 실제 파싱 결과로 자동 전환되며 기존 입력값은 탭 이름이 같으면 유지됨.
- **회원별 보기 테이블 재구성**: 컬럼을 `#·CAS번호·상품·레벨·학년·매칭 가능 교사 및 시간·가능일정·불가능 일정·특이사항·형제` 순(첫수업주간 컬럼 제거, 내부 정렬 키로만 사용)으로 재배치.
  - `productSimpleCell(m)`: "상품" 칸은 원래 상품명 대신 `isJajuMember(m.gubun)` 여부로 "자주표"/"전과목/단과"만 표시(엑셀의 `productCell(m)`은 원본 상품명 그대로 유지, 화면 전용 단순화).
  - `member.displayLevel` 필드 신설(`parseAssignmentFile`): Z열이 "예비초"/"알파벳"이면 매칭용 `level`은 기존처럼 "파닉스"로 정규화하되, 화면 표시용 `displayLevel`은 "알파벳"으로 별도 저장. "레벨" 칸은 `m.displayLevel || m.level`을 "Lv" 접두어 없이, 색 배지 없이 일반 텍스트로 표시.
  - 레벨 필터(`#filterLevel`)를 데이터 기반 동적 목록에서 고정 목록(`LEVEL_FILTER_OPTIONS`)으로 변경. `levelMatchesFilter(m, filter)`가 `displayLevel`/`level`을 함께 고려해 매칭(80/100은 'W' 접미사 유무 모두 허용). 목록 항목 중 "베문법"은 이후 UI 5차 개편에서 "문법"으로 이름 변경됨(아래 참고).
  - "매칭 가능 교사 및 시간" 칸에서 매칭된 교사 이름이 다르면 다른 색 박스로 구분(`TEACHER_COLOR_PALETTE`/`teacherColorStyle`). 마감임박(7/8)은 박스에 노란 강조 테두리(box-shadow) 추가. 이 렌더링은 `renderTeacherSlotsHtml(r, cap)`로 공통화해 회원별 보기(cap=6)와 임의배정의 "추가 배정 가능 회원"(cap=0, 전체 표시)에서 함께 사용.
- **교사별 보기(`renderTeacherPanels`)**: 교사 레벨 태그와 배정 가능 회원의 레벨 칸에서 "Lv" 접두어 제거(레벨/`displayLevel`만 표시). "교사 가능시간" 칸에 `시간표 보기` 버튼을 누르지 않아도 `briefSlotText(mt)`가 "월7:00/월7:30/..." 형식으로 상위 6개까지 간략 표시(버튼은 그대로 유지).
- **슬롯(요일·시간 칸)별 레벨 게이트** — "교사의 시간표 보기를 누르면 모든 레벨이 다 보여진다"는 버그 리포트 대응:
  - `parseSchedulerFile`(genN 제외)에서 슬롯별로 이미 배정된 회원들의 실제 레벨을 `slotLevelSets`로 집계하고, `normalizeLevelForMatch()`(예비초/알파벳→파닉스)로 정규화한 뒤 각 `availableSlots` 엔트리에 `slotLevel`(배열) 필드로 붙임. **레벨 소스 컬럼은 UI 5차 개편에서 I열로 수정됨(아래 참고, 최초 구현 당시엔 영어=H열/일반=G열로 잘못 가정했었음).**
  - `matchAll`에서(요일/시간 필터 통과 후) `slot.slotLevel`이 비어있지 않으면 그 배열에 포함된 레벨의 신규 회원만 매칭 허용(칸이 비어있으면 게이트 없이 레벨 무관 매칭) — genN은 레벨이 아닌 학년/A·B주 모델이라 게이트 미적용.
  - 매칭된 `teacherSlots` 엔트리에도 `slotLevel`을 그대로 실어 보내 모달/버튼에서 "그 칸에 실제로 기록된 레벨"을 그대로 표시할 수 있게 함.
  - 가능 시간대 캘린더 모달: `slotLevelPrefix`(탭 전체 레벨 기준) 대신 `slotLevelText(sl)`(그 칸 고유 레벨, 비어있으면 "레벨무관")를 사용하도록 `matchSlotLabel`/`teacherSlotLabel` 재작성. "Lv" 접두어 전체 제거.
  - ⚠️ (해결됨, UI 5차 개편 참고) 최초 구현 당시엔 실제 스케줄러 파일 없이 합성 데이터로만 검증했고 레벨 소스 컬럼(H/G열)이 잘못 가정되어 있어 실제 파일에서 "레벨무관"이 과도하게 나오는 버그가 있었음. 사용자 피드백으로 실제 컬럼(G=요일/H=시간/I=레벨/M=상품)을 확인해 수정함.
- **임의배정 결과 재구성**:
  - `pickAssignedSlot(mt)`: 매칭된 슬롯 중 요일→시간 순으로 가장 이른 슬롯을 그 회원의 "배정시간"으로 자동 선택(마감임박 우선배정 회원은 그 마감임박 슬롯 자체를 사용). `randomAssign()`이 배정 시점에 `r._assignedSlot`으로 저장.
  - 표 컬럼을 `#·CAS회원번호·레벨·수업주차·코칭구분·배정시간·가능일자·그 외 가능시간·특이사항`으로 재구성(불가능시간/형제 컬럼 제거). "배정시간"은 실제 배정된 요일/시간을 굵게 표시.
  - "교사 가능시간" 버튼을 "그 외 가능시간"으로 이름 변경하고, `openSlotModal(cas, tabName, excludeKey)`/`buildSlotGridHtml(teacher, mt, excludeKey)`에 `excludeKey`(배정된 "요일-시간")를 추가로 넘겨 그 칸은 빨간 강조/⭐ 대상에서 제외하고 "✅ 이미 배정된 시간"으로 회색 표시. 나머지 매칭 가능한 시간만 빨간색으로 표시됨.
  - "⚠️ 미배정 회원" 섹션을 "🙋 추가 배정 가능 회원"로 교체: 매칭되는 교사/시간이 전혀 없는 회원(완전 매칭불가)은 제외하고, 이번 라운드에서 뽑히지 않았지만 아직 배정 가능한 회원만 표시. "교사 가능시간" 칸은 버튼 대신 `renderTeacherSlotsHtml(r, 0)`로 매칭된 모든 교사의 모든 가능 요일/시간을 색상 구분해서 전부 나열.
  - `lastRandomAssign.unassigned`는 이제 "매칭 가능하지만 이번 라운드에 못 뽑힌 회원"만 담음(완전 매칭불가 회원은 제외 — 이들은 엑셀 "배정불가회원" 시트에 이미 별도로 포함되므로 중복 아님). 엑셀 다운로드의 시트 구성/헤더 자체는 이번에도 변경하지 않음(화면 전용 변경).

## UI 3차 개편 (매칭 교사 박스 통합 · 인원순 정렬 · 추가 배정 가능 회원 캘린더 수동 추가)

- **"매칭 가능 교사 및 시간" 렌더링을 교사(탭) 단위로 그룹화**: `renderTeacherSlotsHtml(r, opts)`를 슬롯(요일·시간) 하나당 박스 하나였던 방식에서 **교사(mt) 하나당 박스 하나**로 재작성. 박스 안에 그 교사에게 가능한 모든 요일/시간을 `"월7:00 3/8명 / 월7:30 1/8명 / 금7:00 5/8명"` 식으로 한 줄씩 나열. 교사 이름별 색상 구분(`teacherColorStyle`)은 유지.
  - opts: `cap`(표시할 교사 박스 최대 개수, 0/생략=전체), `collapseTimes`(시간이 3개 이상이면 첫 시간만 보이고 `▼ 외 N개` 버튼으로 펼침 — `toggleTimesBox()`), `clickable`(박스를 누르면 `openAssignPickerModal()` 오픈).
  - 회원별 보기(`renderMemberTable`)는 `{cap:6}`만 사용(펼치기/클릭 없이 항상 전체 시간 표시). 임의배정의 "추가 배정 가능 회원"은 `{cap:0, collapseTimes:true, clickable:true}` 사용.
  - **적게 찬 방부터 위로 정렬**: 박스(교사) 안의 슬롯들을 `currentCount` 오름차순으로 정렬하고, 박스 자체도 그 교사의 최소 `currentCount` 기준 오름차순으로 정렬 — 여러 교사가 매칭될 때 가장 여유 있는 교사/시간이 위쪽에 보임. (회원별 보기·추가 배정 가능 회원 모두 공통 적용)
- **"추가 배정 가능 회원" 캘린더에서 바로 임의 배정 결과에 추가**: 박스를 클릭하면 `openAssignPickerModal(cas, tabName)`이 `buildAssignPickerHtml()`로 그 회원×교사 조합의 요일×시간 캘린더를 그림(빨간 테두리 칸=배정 가능 시간, 클릭하면 `selectAssignSlot()`이 `.assign-pick-cell.selected`로 표시하고 우측 하단 `➕ 추가` 버튼을 활성화). 버튼(`confirmAssignPick()` → `assignMemberManually(cas, tabName, day, time)`)을 누르면:
  - 선택한 슬롯을 `r._assignedSlot`으로 저장하고 `r._manuallyAdded = true` 플래그를 세운 뒤 `lastRandomAssign.perTeacher[tabName].picked`에 강제로 추가(교사별 설정 인원 캡 무시 — 사용자가 수동으로 추가하는 것이므로 의도적으로 우회).
  - `lastRandomAssign.unassigned`(추가 배정 가능 회원 목록)에서 해당 회원을 제거.
  - 모달을 닫고 `renderRandomAssignResultUI()`로 다시 그림.
  - `randomAssign()`은 계산 로직(라운드로빈/마감임박 우선배정)과 렌더링을 분리해, 계산 후 `lastRandomAssign`을 저장하고 `renderRandomAssignResultUI()`를 호출하는 구조로 리팩터링됨 — 이 함수는 `lastRandomAssign` 전역 상태만 보고 다시 그리므로 수동 추가 후에도 재사용 가능.
  - 화상코칭거부 회원은 시간 선택 없이 모달에 바로 `➕ 추가` 버튼만 표시(`assignMemberManually(cas, tabName, null, null)`).
  - 임의배정결과 표에서 수동으로 추가된 행은 `tr.manually-added-row`(빨간 굵은 outline + 옅은 빨간 배경) + `🆕 추가배정` 배지로 강조되어 자동 배정된 회원과 구분됨.
- 이번 변경은 모두 화면(웹 UI) 전용이며 엑셀 다운로드는 이번에도 변경하지 않음(단, 수동으로 추가한 회원도 `lastRandomAssign.perTeacher[...].picked`에 포함되므로 엑셀 "임의배정결과" 시트에는 자동으로 함께 반영됨 — 배지/강조 스타일만 엑셀엔 없음).

## UI 4차 개편 (매칭 교사 박스 컴팩트화 · 교사별 레벨 제한 체크박스)

- **"매칭 가능 교사 및 시간" 박스 컴팩트화**: 회원별 보기에서 이 컬럼이 너무 길어져 같은 화면에 가능일정·불가능일정·특이사항·형제 컬럼까지 다 보이지 않는다는 문제 대응. `renderTeacherSlotsHtml(r, opts)`의 박스 내부를 `"월7:00 3/8" / "월7:30 1/8"` 같은 줄글 나열 대신 작은 `.time-chip` 배지들이 `.time-chip-wrap`(flex-wrap) 안에서 줄바꿈되도록 재작성. 교사 이름(`.teacher-name-cell`)은 박스 왼쪽 중앙에 고정(`align-self:center`)하고, 시간 칩들은 오른쪽에 붙어 필요시 아래로 줄바꿈됨(`justify-content:flex-end`). 컬럼 `<td>`도 `min-width:200px`(무제한 확장) → 고정폭으로 변경해서 옆 컬럼들이 잘리지 않게 함. 회원별 보기(`renderMemberTable`)와 임의배정의 "추가 배정 가능 회원" 양쪽 다 적용. 접기/펼치기(`collapseTimes`)·클릭(`clickable`) 옵션 동작은 기존과 동일하게 유지. **(UI 5차 개편에서 폭 260px→210px 축소 + 시간 칩을 가로 줄바꿈에서 세로 나열로 재변경됨, 아래 참고)**
- **교사별 레벨 제한(체크박스) — "이 교사에겐 이 레벨만" 규칙을 실제 매칭에 반영**: 기존에 "④ 추가 지침 입력"(구 ③) 텍스트박스에 "○○ 교사는 △△ 레벨만 배정해주세요" 같은 문구를 적어도 실제 매칭 결과에는 전혀 반영되지 않던 문제 대응.
  - **설계 결정(사용자가 직접 선택)**: 자유 텍스트를 파싱해서 규칙을 추론하는 방식(줄 단위 간단 문법 포함)은 오매칭 위험이 있어 채택하지 않음 — `AskUserQuestion`으로 "간단한 줄 단위 규칙(추천)" / "자유 문장 최대한 인식" / "별도 체크박스·선택 UI" 세 가지를 제시했고, 사용자가 **"별도 체크박스/선택 UI"를 명시적으로 선택**함에 따라 체크박스 방식으로 구현. 이에 따라 "④ 추가 지침 입력" 텍스트박스는 **매칭에 전혀 영향을 주지 않는 참고용 메모**로 명확히 재정의(placeholder/안내문구 갱신)하고, 실제 규칙은 새 카드에서 설정하도록 UI를 분리함.
  - 파일 업로드 카드 바로 아래, "② 교사별 임의 배정 인원 설정" 카드 다음에 **"③ 교사별 레벨 제한 설정"** 카드(`#teacherLevelRestrictWrap`) 신설. 교사별 인원 설정과 마찬가지로 **비교 시작 전에도** `[🔍 교사별 회원 수 설정]` 버튼(`loadTeacherNamesForTargets()`)으로 미리 채워짐(`renderTeacherLevelRestrictions()` 호출 추가).
  - 각 교사 박스에 `LEVEL_FILTER_OPTIONS`(알파벳/파닉스/1~5/80/100/문법) 체크박스가 나열됨. 하나도 체크 안 하면(기본값) 그 교사는 **기존 방식 그대로 제한 없이** 매칭(교사 스케줄러 탭 자체의 `teacher.levels` 기준). 하나 이상 체크하면 그 레벨(들)의 회원만 그 교사와 매칭 가능.
  - **상태**: `teacherLevelRestrictions` 전역(`{tabName: Set<허용레벨문자열>}`). `toggleTeacherLevelRestriction(el)`이 체크박스 change마다 이 Set을 갱신하고, 배지("2,3만")를 갱신하기 위해 카드를 다시 그린 뒤(`renderTeacherLevelRestrictions()`), 이미 비교를 실행한 상태면 `reapplyMatchingIfReady()`가 `matchAll()`을 즉시 재실행하고 `renderResults()`로 전체 화면(회원별 보기·교사별 보기·임의배정 결과 등)을 다시 그려서 **체크 즉시 모든 화면에 반영**됨(재업로드/재비교 불필요).
  - **매칭 로직 적용 지점**: `levelMatchesFilter()`(레벨 필터 드롭다운과 동일한 매칭 함수, 로직 중복 없이 재사용)를 그대로 써서 `teacherAllowsMemberLevel(tabName, member)`을 구현하고, `matchAll()`의 `teachers.forEach(teacher => {...})` 루프 맨 앞에서 `if (!teacherAllowsMemberLevel(teacher.tabName, member)) return;`로 게이트 — 제한에 걸리면 그 교사는 이 회원의 매칭 후보 목록에서 아예 제외됨(화상코칭거부/슬롯레벨게이트 등 기존 로직보다 먼저 적용).
  - `resetAll()`은 `teacherLevelRestrictions = {}`로 초기화하고 카드도 안내 문구 상태로 재렌더링.
  - Playwright(`smoke_test6.js`)로 검증: 제한 전엔 레벨2·레벨3 회원 모두 특정 교사와 매칭 → 그 교사에 "3"만 체크 → 레벨2 회원은 매칭에서 즉시 제외되고(false) 레벨3 회원은 유지(true), 카드 배지("3만") 표시, 교사별 보기도 오류 없이 재렌더링 → 체크 해제하면 레벨2 회원 매칭 복원 → `resetAll()` 후 상태·UI 모두 초기화. `test_scenario.js`/`test_scenario2.js` 회귀 테스트도 재실행해 기존 eng 자주표/genN 로직에 영향 없음을 확인(제한을 설정하지 않으면 `teacherAllowsMemberLevel`이 항상 true를 반환하므로 동작 변화 없음).
  - ⚠️ **실제 데이터 파일로 검증되지 않음** — Node 기반 합성(mock) 데이터로 체크박스 UI와 매칭 게이트의 동작만 확인했고, 실제 교사 탭 이름·레벨 표기가 `LEVEL_FILTER_OPTIONS`와 정확히 일치하는지, 교사 수가 많을 때 카드가 실사용에 불편하지 않은지는 실제 파일로 재확인 필요.

## UI 5차 개편 (스케줄러 컬럼 매핑 수정 · 레벨필터 이름변경 · 매칭박스 세로나열 · 임의배정 레벨그룹핑)

- **개별 스케줄러 탭의 요일/시간/레벨/상품 컬럼 매핑 수정** — `parseSchedulerFile`(genN 제외)이 지금까지 요일=I열(idx8)·시간=J열(idx9)·레벨(영어)=H열(idx7)·레벨(일반,학년)=G열(idx6)로 잘못 가정하고 있었음. 사용자가 실제 파일 기준으로 확인해준 대로 **요일=G열(idx6), 시간=H열(idx7), 무슨 수업(레벨/학년)=I열(idx8)** 로 수정. 상품 정보는 **M열(idx12)**에 있다는 것도 확인해, 슬롯별로 이미 기록된 상품을 `slotProductSets` → `availableSlots[].slotProduct`(배열)로 새로 집계함(레벨과 동일한 방식, `slotLevelSets`/`slotLevel` 옆에 병행). **(⚠️ UI 6차 개편에서 재수정됨 — 아래 참고. 이 매핑도 잘못된 것으로 확인되어 요일=H열/시간=I열/레벨=G열로 다시 수정함.)**
  - **"레벨무관" 과다 노출 버그의 원인**: 위 컬럼 매핑이 틀려서(H/G열을 읽었지만 실제 레벨 데이터는 I열에 있었음) 슬롯별 레벨 게이트(`slotLevel`)가 항상 빈 배열로 집계되어, 교사 시간표 모달에서 이미 학생이 있는 칸까지 전부 "레벨무관"으로 표시되던 것. 컬럼을 바로잡아 실제 레벨이 `slotLevel`에 정확히 채워지도록 해결.
  - `slotLevelText(sl)`/`matchSlotLabel(sl)`/`teacherSlotLabel(slot)`에 `slotProductText(sl)` 결과(그 칸에 기록된 상품, 있으면)를 작은 보조 줄(`font-size:9px`)로 함께 표시하도록 확장 — 가능 시간대 캘린더 모달(`buildSlotGridHtml`)과 "추가 배정 가능 회원" 시간 선택 모달(`buildAssignPickerHtml`) 양쪽에 반영됨. 회원별 보기·추가배정가능회원의 작은 시간 칩(`.time-chip`)에는 공간이 부족해 상품 정보를 넣지 않음(요일/시간/인원만 유지).
  - `parseTeacherTabGenN`(genN 전용, CN1형 탭)은 이번 수정 대상이 아님 — 원래부터 G열=요일/H열=시간을 쓰고 있어 문제 없었음(M열은 genN에서는 주차구분 용도로 이미 사용 중이라 상품과 무관).
  - 회귀 확인을 위해 `test_scenario.js`/`test_scenario2.js`의 스케줄러 mock 행(`schedRow`)도 새 컬럼 인덱스(G/H)에 맞춰 갱신하고 재실행 — 자주표 A/B주 용량 계산 결과가 이전과 동일하게 나오는 것을 확인함.
- **레벨 필터 "베문법" → "문법"으로 이름 변경**: `LEVEL_FILTER_OPTIONS`의 마지막 항목과 `levelMatchesFilter`의 해당 분기(`filter === '문법'`)를 변경. 매칭 로직(느슨하게 "문법" 포함 여부로 판단)은 그대로 유지, 표시 이름만 변경.
- **"매칭 가능 교사 및 시간" 박스를 다시 세로 나열로 변경**: UI 4차에서 시간 칩을 가로로 나열하다 넘치면 줄바꿈(`flex-wrap`)하던 것을, "교사 이름은 왼쪽 가운데, 가능 시간은 오른쪽에 세로로" 요청에 맞춰 `.time-chip-wrap`을 `flex-direction: column`으로 변경(각 시간 칩이 한 줄씩 아래로 쌓임). 컬럼 폭도 260px→210px로 줄임(세로 나열이라 가로 폭이 덜 필요해짐). 교사가 다르면 다른 색 박스로 구분되는 동작(`teacherColorStyle`)은 그대로 유지.
  - "추가 배정 가능 회원"의 `▼ 외 N개` 펼치기 버튼(`collapseTimes`)도 같은 `.time-chip-wrap`/`.times-full` 구조를 쓰므로, 펼쳤을 때 시간들이 자동으로 세로 나열됨(별도 로직 변경 없이 CSS만으로 해결 — `.times-full`도 `display:flex;flex-direction:column`으로 지정).
- **임의 배정 결과를 레벨 순으로 묶어서 표시**: `levelGroupKey(m)` 헬퍼(레벨을 `LEVEL_FILTER_OPTIONS` 순서의 인덱스로 변환, 목록에 없는 값은 맨 뒤)를 추가하고, `renderRandomAssignResultUI()`의 교사별 배정 테이블에서 `picked` 배열을 이 순서로 정렬한 뒤, 레벨이 바뀔 때마다 "레벨: N" 회색 구분 행을 표에 삽입. 교사별 배정 인원수·순번(`#`) 등 기존 표시는 그대로 유지(순번은 레벨 정렬 후 순서 기준으로 다시 매겨짐).
- Playwright(`smoke_test7.js`)로 검증: 레벨 필터 목록에 "문법"만 있고 "베문법" 없음, `.time-chip-wrap`의 `flex-direction`이 `column`, 교사 이름 `align-self:center`, 임의배정 결과에 "레벨: 2"/"레벨: 3" 구분 행이 순서대로(2가 3보다 먼저) 나타남을 확인. 스케줄러 파싱 단위 테스트(vm 샌드박스)로 G/H/I/M열을 채운 mock 행을 넣었을 때 `slotLevel`이 정확히 채워지고 "레벨무관"이 더 이상 뜨지 않음도 별도 확인. `test_scenario.js`/`test_scenario2.js`/`smoke_test4~6.js` 전부 재실행해 회귀 없음 확인.
  - ⚠️ **실제 파일로 최종 검증 필요** — 이번에도 사용자 설명(G/H/I/M열)을 근거로 컬럼 인덱스를 수정했을 뿐, 실제 개별 스케줄러 파일을 업로드해서 교사 시간표 모달의 레벨·상품 표시가 올바르게 나오는지는 아직 확인하지 못함.

## UI 6차 개편 (스케줄러 컬럼 재수정 · 상품 3분류 · 상품 필터 · 회원별 보기 정렬/색상/접기)

- **개별 스케줄러 컬럼 매핑 재수정 (H=요일·I=시간·G=레벨/반이름)** — UI 5차에서 G=요일/H=시간/I=레벨로 고쳤음에도 "여전히 교사 가능 시간이 반영 안 된다"는 재확인 피드백을 받고, 사용자가 다시 한번 명확히 확인해준 정확한 매핑으로 재수정: **요일=H열(idx7), 시간=I열(idx8)** (이미 그 교사와 수업 중인 회원의 요일/시간), **레벨(그 레벨을 수업하는 반 이름)=G열(idx6)**. 상품(M열, idx12)은 그대로 유지. `parseSchedulerFile`(genN 제외)의 `day`/`time`/`memberLevel` 추출 부분만 수정, `parseTeacherTabGenN`(CN1형 탭)은 이번에도 대상 아님(원래부터 G=요일/H=시간을 정확히 쓰고 있었음).
  - `test_scenario.js`/`test_scenario2.js`의 `schedRow` mock도 새 인덱스(H=idx7 요일, I=idx8 시간)로 갱신 후 재실행, A/B주 용량 계산 결과가 이전과 동일함을 확인. vm 샌드박스 단위 테스트로 G/H/I열을 채운 mock 행을 넣었을 때 `slotLevel`이 G열 값으로 정확히 채워짐도 별도 확인.
  - ⚠️ 컬럼 매핑을 두 번째로 수정하는 것이라 이번에도 실제 파일로 최종 확인이 특히 중요함 — 사용자가 실제 스케줄러 파일을 올려서 교사 시간표가 올바르게 반영되는지 다음 기회에 꼭 확인 필요.
- **상품 3분류 체계 도입 (전과목/단과/자주표)** — 기존에는 화면에 "전과목/단과"가 하나로 합쳐져 표시되고, 자주표 판별도 구분(B열) 텍스트에만 의존했음. 사용자가 알려준 정확한 상품명(I열, `COL.PRODUCT`) 패턴 기준으로 `productCategory(rawProduct)` 함수 신설: 정확히 "캐츠잉글리시"면 **단과**, "캐츠잉글리시+캐츠홈"을 포함하고 "자기주도학습표"가 있으면 **자주표**, 그 외 "캐츠홈" 포함이면 **전과목** (약정기간 "2년" 등은 `stripYear`로 먼저 제거하고 비교). 패턴에 안 맞는 상품(gen/genN 관리교사 상품 등)은 빈 문자열(미분류) 반환.
  - `memberProductCategory(m)`: `m.isJaju`가 true면 무조건 '자주표', 아니면 `productCategory(m.product)` 사용 — 화면 표시(`productSimpleCell`)와 새 상품 필터 양쪽에서 공통으로 사용.
  - **자주표(`isJaju`) 판별에 상품명 기준 추가**: `parseAssignmentFile`/`buildFullMemberIndex` 모두 `isJajuMember(gubunVal) || productCategory(product) === '자주표'`로 변경 — 구분(B열) 텍스트와 상품명(I열) 패턴 둘 중 하나만 맞아도 자주표로 인정(상호 보완). 기존에 CLAUDE.md에 "B열 실제 표기가 다를 수 있다"고 남겨뒀던 불확실성에 대한 안전장치로, 상품명이 확실한 신호일 때 놓치지 않도록 함. `gen`/`genN` 모드는 이 로직 대상이 아니라 영향 없음(기존처럼 `isEngMode` 조건 안에서만 계산).
  - `productSimpleCell(m)`(회원별 보기 "상품" 칸)이 `memberProductCategory(m)`을 그대로 표시하도록 변경(패턴이 안 맞는 미분류 상품만 기존처럼 "전과목/단과"로 폴백 표시).
- **회원별 보기에 "상품" 필터 추가**: 레벨 필터 옆에 `#filterProductCat` 드롭다운(전체/전과목/단과/자주표) 신설. `applyFilters()`/`renderMemberTable()`에 `productCatFilter` 파라미터를 추가해 `memberProductCategory(m)`으로 필터링. 기존 "상품 선택" 체크박스 그리드(`selectedProducts`/`productMatch`, ★배정 파일의 원본 상품명 전체를 나열해 파싱 시점에 회원을 아예 제외하는 용도)와는 별개 기능 — 이건 파싱된 결과를 화면에서 3분류로 다시 걸러보는 필터.
- **회원별 보기 정렬 개선**: 기존 "매칭 가능 교사 수 오름차순" 정렬은 그대로 유지(매칭없음이 자동으로 맨 위)하되, 동점자 처리를 "먼저 들어온 순서 유지(stable sort)" 대신 **레벨 오름차순**(`levelGroupKey`, `LEVEL_FILTER_OPTIONS` 순서: 알파벳,파닉스,1,2,3,4,5,80,100,문법)으로 명시적으로 정렬하도록 2차 정렬 키 추가.
- **"매칭 가능 교사 및 시간" 시간 4개 이상 접기 (회원별 보기 전용)**: `renderTeacherSlotsHtml(r, opts)`에 `collapseThreshold`(기본 3)·`collapseShow`(기본 1) 옵션을 추가해 접기 기준/노출 개수를 호출부마다 다르게 지정할 수 있도록 일반화. 회원별 보기(`renderMemberTable`)는 `{cap:6, collapseTimes:true, collapseThreshold:4, collapseShow:4}`로 호출해 시간이 4개 이상이면 4개까지 보여주고 `▼ 외 N개` 버튼으로 나머지를 펼쳐볼 수 있게 함. "추가 배정 가능 회원"(임의배정)은 기존과 동일하게 3개 이상→1개만 노출 유지(변경 없음). `.times-short`도 `.times-full`과 동일하게 `display:flex;flex-direction:column`으로 지정해, 접힌 상태에서 여러 개(4개)가 보일 때도 세로로 나열되도록 CSS 보강.
- **"매칭 가능 교사 및 시간" 교사별 색상을 전역으로 고정**: 기존에는 `renderTeacherSlotsHtml` 호출마다(=행마다) `new Map()`으로 색상 매핑을 새로 만들어서, 같은 교사라도 행(회원)마다 팔레트 순번이 달라져 색이 오락가락했음(한 행 안에서는 서로 다른 교사가 다른 색으로 보였지만, 표 전체를 훑어볼 때 같은 교사를 색으로 추적하기는 어려웠음). 전역 `teacherColorMapGlobal`(모듈 레벨 `Map`)을 도입해 모든 행이 이 맵을 공유하도록 변경 — 이제 같은 교사는 회원별 보기 표 전체에서 항상 같은 색 박스로 보임. `resetAll()`에서 `teacherColorMapGlobal = new Map()`으로 초기화(새 비교 시작 시 색 배정을 리셋).
- Playwright(`smoke_test8.js`)로 검증: 시간 5개인 교사 박스가 4개까지만 보이고 "외 1개" 버튼으로 표시됨, `teacherColorStyle`이 같은 교사 이름에 항상 같은 색을 반환하고 다른 교사와는 다른 색을 반환함, 매칭없음 회원이 맨 위이고 그 다음 레벨 오름차순으로 정렬됨(레벨2 → 레벨3), 상품 필터(전과목/단과/자주표) 각각 선택 시 해당 회원만 표시됨을 확인. Node vm 샌드박스 단위 테스트로 `productCategory()`의 3가지 패턴("캐츠잉글리시"→단과, "캐츠잉글리시+캐츠홈 2년(N)"→전과목, "캐츠잉글리시+캐츠홈(자기주도학습표)"→자주표, 미분류 상품→빈 문자열)과 새 스케줄러 컬럼 매핑(H=요일/I=시간/G=레벨) 결과를 모두 확인. `test_scenario.js`/`test_scenario2.js`/`smoke_test4~7.js` 전부 재실행해 회귀 없음 확인.
  - ⚠️ **실제 데이터 파일로 검증되지 않음** — 이번에도 사용자 설명을 근거로 한 컬럼 재매핑과 상품명 패턴 매칭이라, 실제 개별 스케줄러 파일과 실제 ★배정 파일의 상품명 표기가 여기 기술된 패턴과 정확히 일치하는지 확인이 특히 중요함. 실제 상품명에 다른 변형(예: "캐츠잉글리시+캐츠홈 1년(자기주도학습표)" 외의 다른 표기)이 있다면 `productCategory()` 분류가 어긋날 수 있음.

## UI 7차 개편 (캘린더 모달 확대 · 레벨 Lv 표기 복원 · A/B주 세로 표시)

- **가능 시간대 캘린더 모달 크기 확대**: 요일×시간 캘린더가 세로 스크롤이 필요할 정도로 좁았던 문제 대응. `.modal-box`의 `max-width: 720px → 1100px`(`width: 100% → 96%`), `max-height: 85vh → 96vh`로 확대. 모달 안의 테이블은 `.modal-box td/th { padding:4px !important; font-size:11px !important }`로 셀을 더 촘촘하게 만들어(각 셀의 인라인 style보다 우선 적용되도록 `!important` 사용), 넓어진 모달 안에서 12개 시간대(3:00~8:30) × 5요일이 스크롤 없이 한 화면에 들어오도록 함. `buildSlotGridHtml`/`buildAssignPickerHtml` 두 모달 모두 같은 `.modal-box` 클래스를 쓰므로 함께 적용됨.
- **캘린더 방 레벨에 "Lv" 접두어 복원**: 회원별 보기 등 표 안의 레벨 텍스트는 UI 2차 개편에서 "Lv" 접두어를 뺐지만(별도 요청이었음), 이번엔 **캘린더 모달 안의 방 레벨 표시**에 한해 "Lv1"처럼 다시 붙여달라는 요청. `slotLevelText(sl)`을 숫자로만 된 레벨(1~5/80/100)은 `Lv${v}`로, 파닉스/알파벳/문법처럼 이름으로 된 레벨은 그대로(접두어 없이) 표시하도록 수정 — "레벨무관"도 그대로 유지. 이 함수를 쓰는 `matchSlotLabel`/`teacherSlotLabel`(즉 두 캘린더 모달의 모든 셀)에 자동으로 반영됨.
- **A주/B주 방을 가로가 아니라 세로로 표시**: 참고용(회색) 셀에서 A트랙/B트랙 인원을 함께 보여주는 `teacherSlotLabel(slot)`이 기존엔 `"Lv2(A) 1/8 · Lv2(B) 2/8"`처럼 가운데 점(·)으로 가로 나열했던 것을, `"<div>Lv2(A) 1/8</div><div>Lv2(B) 2/8</div>"`로 바꿔 각 트랙이 한 줄씩 세로로 쌓이도록 변경(회원에게 실제로 열려있는 매칭 셀 `matchSlotLabel`은 애초에 A 또는 B 한 트랙만 표시하므로 변경 대상 아님).
- Playwright(`smoke_test9.js`)로 검증: `openSlotModal()`로 연 모달의 `.modal-box` 계산된 `max-width`가 1100px, `max-height`가 96vh(기본 뷰포트 기준 691.2px)로 커진 것을 확인. 모달 본문에 "Lv2" 텍스트가 나오고 레벨 정보 없는 칸은 여전히 "레벨무관"으로 남는 것을 확인. `teacherSlotLabel()`을 직접 호출해 반환된 HTML에 `<div>` 두 개가 각각 "Lv2(A) 1/8"/"Lv2(B) 2/8"로 들어있고 "·" 구분자가 없음을 확인. `test_scenario.js`/`test_scenario2.js`/`smoke_test4~8.js` 전부 재실행해 회귀 없음 확인.
  - ⚠️ 실제 파일로 캘린더가 실제로 한 화면에 다 들어오는지(모니터 해상도에 따라 달라질 수 있음), 레벨 Lv 표기가 실제 레벨 값과 잘 맞는지는 아직 사용자 화면에서 직접 확인되지 않음.

## UI 8차 개편 (캘린더 모달 가로 스크롤 재수정 · A/B주 레벨 개별 표시 · 요약 카드 정리)

UI 7차 개편(모달 크기 확대)을 적용했음에도 "교사별 보기 → 시간표 보기" 모달에서 월~금이 여전히 한 화면에 안 보이고 가로로 스크롤해야 한다는 재보고를 받고 원인을 재조사함.

- **가로 스크롤 재발 원인**: `.modal-box`의 `max-width`/`table-layout:fixed` 자체는 정상 동작해서(표(table) 요소 자체는 `table-layout:fixed`로 컨테이너 폭에 맞춰짐) 표는 넘치지 않지만, 각 셀 안의 값(`matchSlotLabel`/`teacherSlotLabel`이 반환하는 "Lv4(A) 7/8" 같은 텍스트와 상품명 서브텍스트)에 `white-space:nowrap`이 걸려 있어서 **셀보다 텍스트가 길면 줄바꿈되지 않고 셀 밖으로 삐져나가며** 그 삐져나온 만큼 스크롤 컨테이너(`overflow-x:auto` div)의 `scrollWidth`가 커져 가로 스크롤이 발생함. 좁은 화면(예: 뷰포트 768px 이하)이거나 상품명이 길 때(예: "캐츠잉글리시+캐츠홈 2년(N)") 특히 잘 재현됨 — Playwright로 1280px 뷰포트·짧은 테스트 데이터로는 재현되지 않았지만, 768px·긴 상품명 조합으로 실제로 9px 정도의 가로 오버플로우를 재현해 원인을 특정함.
  - 조치: `buildSlotGridHtml`/`buildAssignPickerHtml`의 값 칸(빨간 매칭 칸·회색 참고 칸) 스타일을 `white-space:nowrap` → `white-space:normal;word-break:keep-all;overflow-wrap:break-word`로 변경. 이제 셀 안 텍스트가 길면 셀 폭(고정 열 너비) 안에서 자연스럽게 줄바꿈되어(한글은 단어 단위 유지, 불가피하면 강제 줄바꿈) 세로로 길어질 뿐 가로로는 절대 넘치지 않음. 맨 왼쪽 "시간" 라벨 칸(3:00 등 짧고 고정된 텍스트)만 기존처럼 `white-space:nowrap` 유지.
  - Playwright로 480px~1280px 6단계 뷰포트 + 60칸 전체 채운 데이터 + 긴 상품명("캐츠잉글리시+캐츠홈 2년(N) 정기구독 특별할인상품")으로 재검증: 모든 뷰포트에서 `scrollWidth === clientWidth`(가로 오버플로우 없음) 확인 (`smoke_test10.js`, `smoke_test11.js`).
- **A주/B주 방 레벨을 각각 다르게 표시**: 같은 요일-시간 칸이라도 A주에 열리는 반과 B주에 열리는 반의 레벨(반 이름)이 다를 수 있다는 사용자 지적 반영. 기존엔 `slotLevelSets`(A/B 구분 없이 합친 레벨 집합) 하나만 있어서 `teacherSlotLabel`이 A줄/B줄에 항상 같은 레벨을 표시했음. `parseSchedulerFile`에 `slotLevelSetsA`/`slotLevelSetsB`를 신설해 개별 스케줄러 행을 파싱할 때 그 행이 A주 자주표 회원(`fullMemberIndex`의 `abWeek==='A주'`)이면 A쪽에, B주 자주표 회원이면 B쪽에, 매주(전과목/단과) 회원이면 양쪽 모두에 레벨을 기록하도록 분리(자주표인데 A/B 판별 불가한 경우는 기존처럼 보수적으로 양쪽 모두에 기록). `availableSlots`에 `slotLevelA`/`slotLevelB` 필드를 추가하고, `teacherSlotLabel(slot)`이 두 값을 각각 `slotLevelText()`에 넣어 `"<div>Lv4(A) 7/8</div><div>Lv5(B) 4/8</div>"`처럼 A/B가 서로 다른 레벨로 보이도록 수정. (기존 매칭 게이트용 합친 `slotLevel`은 그대로 유지 — 레벨 필터링 로직 자체는 건드리지 않음.)
  - genN(관리교사 N상품) 모드는 이번 변경 대상 아님 — `parseTeacherTabGenN`은 원래부터 슬롯에 레벨 정보를 기록하지 않아 캘린더에 항상 "레벨무관(A)/레벨무관(B)"로 표시되며, 이 동작은 이번 수정 전후로 동일함.
  - `test_scenario3.js` 신설: vm 기반으로 같은 "월 6:00" 칸에 A주 회원(G열=4)과 B주 회원(G열=5)을 각각 넣어 `parseSchedulerFile`이 `slotLevelA=['4']`, `slotLevelB=['5']`로 정확히 분리하는지, 매주 회원(G열=3)은 A/B 양쪽에 똑같이 `['3']`로 들어가는지 확인. 전부 PASS.
- **요약 카드에서 "분석 교사" 수치 제거**: 화면 상단 "📊 요약" 카드의 "👩‍🏫 분석 교사: N명" 칩을 삭제(사용자가 "없어도 괜찮다"고 확인). 다운로드용 엑셀 "요약" 시트의 "분석 교사수" 행은 그대로 유지(화면 카드와 별개의 파일 산출물이라 이번 요청 범위 밖으로 판단 — 필요하면 추가 요청 시 삭제 가능).
- 회귀 확인: `test_scenario.js`/`test_scenario2.js`/`test_scenario3.js`(신설)/`smoke_test4~11.js`(9는 새 A/B 필드에 맞게 목데이터 보강, 11은 신설) 전부 통과.
  - ⚠️ 이번에도 실제 스케줄러 파일 업로드 후 브라우저 화면으로 최종 확인은 아직 안 됨 — 특히 실제 파일의 상품명 길이·레벨 표기가 테스트에서 가정한 것과 다를 수 있어, 여전히 가로 스크롤이나 A/B 레벨 오표시가 보이면 캡처 화면과 함께 알려주면 좋음.

## 화면 표시 통일 헤더명
- "가능 일정(수기_AJ열)" = AJ열 원문
- "불가능시간(설문 답변)" = AP~AW 종합 (`unavailDisplay`)

## 엑셀 다운로드 시트 구성
요약 → 배정필요회원전체 → 배정불가회원(항상) → (임의배정 실행 시) 임의배정결과 → 미배정회원 → 교사별 탭
- 파일명: `배정결과_YYYY-MM-DD.xlsx` (당일 날짜, `getDateStr` — 로컬 날짜 기준, toISOString 미사용으로 UTC 오차 방지)
- `배정불가회원`: `isUnassignable(r)` = 매칭되는 `hasSlots` 슬롯이 전혀 없는 회원. 임의배정 여부와 무관하게 항상 포함됨 ("미배정회원"=임의배정 라운드에서 못 뽑힌 회원과는 별개 개념).
- 교사별 탭 컬럼 순서: CAS회원번호·학년·레벨·수업주차·상품·단과여부 → [genN: 코칭주기/배정주차] → 가능 시간 조합(현재인원) → 탭명·현재탭가능시간대 → 가능 일정(수기_AJ열)·불가능시간(설문 답변)·특이사항·형제정보
- `productCell(m)`: 구독상품명(I열) 원본 그대로 표시
- `singleCourseCell(m)`: 상품명이 정확히 "캐츠잉글리시"인 경우만 "단과" (별도 "단과여부" 컬럼). "캐츠잉글리시+캐츠홈" 등 결합상품은 단과 아님(빈칸)

## XLSX 라이브러리
SheetJS Community Edition(`xlsx.full.min.js`)은 셀 스타일(`.s`) 쓰기 미지원(Pro 전용)이라
`https://cdn.jsdelivr.net/npm/xlsx-js-style@1.2.0/dist/xlsx.bundle.js`(API 호환 포크)로 교체.

## 진행 이력 (요약)
- 이름 열 제거, 형제회원 정보 표시 수정
- AP/AJ/AQ-AV 가능요일·시간 판단 기준 재설계 (불가능시간 설문 원문 vs 가능일정 수기)
- 첫수업주간 미입력 회원 제외, engJj AX열 필터
- 9~10시 시간대 제거, 회원표 가능요일 열 제거, 헤더명 통일
- 임의배정 라운드로빈(고루 배정), 결과/미배정 표시 항목 보강, 엑셀 탭 추가
- genN 모드 추가: (N)상품 격주회원 판별, CN1형 스케줄러(A주/B주/매주) 파싱, countA/countB 용량 매칭, 화면·엑셀 전반에 코칭주기/배정주차 표시
- 임의배정 인원수 미입력 시 무제한 고루분배, 'AW'→'요청사항' 라벨 변경, 테이블 컬럼 너비 조정, 레벨/학년방 매칭 강화(deriveLevelsFromMembers)
- 가능요일 판단 우선순위 재설계: AJ(가능일정 수기) 우선, AJ에 정보 없을 때만 AP(불가능시간 설문 답변) 보조 (`getApAnalysis` 분리)
- 레벨/코칭주기 배지 줄바꿈 처리, 교사별 보기에 "교사 가능시간" 컬럼 추가(가능요일 컬럼 대체) 및 컬럼 순서 재배치
- 임의배정에 마감임박(7/8) 우선 배정 로직 추가, 엑셀(xlsx-js-style 교체) 전반에 마감임박 노란색 강조 적용
- "수업주차"(=첫수업주간, `firstWeekCell`) 컬럼을 회원별/교사별/임의배정/미배정 화면 표 및 엑셀(임의배정결과·미배정회원·교사별 탭)에 추가
- 엑셀 파일명에 당일 날짜 정확히 반영 (`getDateStr`, 로컬 날짜 기준)
- "배정불가회원" 엑셀 시트 신설 (매칭 가능한 교사/시간이 전혀 없는 회원, 임의배정 여부와 무관하게 항상 포함)
- 교사별 탭 엑셀 컬럼을 회원번호/학년/레벨/수업주차/상품/단과여부/가능 시간 조합 순으로 재구성
- "단과여부" 컬럼 분리: 상품명이 정확히 "캐츠잉글리시"일 때만 "단과" 표시 (캐츠잉글리시+캐츠홈 등 결합상품 제외), 상품 컬럼은 원본 그대로
- 그룹영어(eng/engJj) 자주표(자기주도표) A주/B주 격주 배정 로직 추가: B열(구분)='자주표' 판별, R열(첫수업주간) 날짜로 A/B주 계산(`getAbWeek`, 기준일 2026-08-24=B주), ★배정 전체행 색인(`fullMemberIndex`/`buildFullMemberIndex`)으로 이미 배정된 회원의 A/B주 역조회, 트랙당 최대 2명+전체 8명 정원 매칭, 트랙 꽉 찼을 때 반대 트랙으로 자동 이동(1주 늦게 시작). 예비초/알파벳 레벨→파닉스 통합, 초등6/예비초 학년→"관리교사겸함" 안내 배지도 함께 추가.
- UI 개편 14건 일괄 반영(위 "UI 개편" 섹션 참고): 그룹영어/영어 자주표 모드 통합, 요약 카드 색상 단순화 + 매칭 어려움 클릭 시 명단 표시, 전체 가능 슬롯 칩 삭제, 회원별 보기 상품/레벨 컬럼 분리 및 순서 재배치, 학년 "초등" 글자 제거, 첫수업주간 날짜 포맷(YY-MM-DD), 매칭 교사 적은 순 정렬(미매칭 최상단), 교사별 임의 배정 인원(교사 이름별 입력), 가능 시간대 캘린더 모달(레벨(A/B) 표기 + 최이른 슬롯 ⭐ 추천) 신설 및 기존 텍스트 나열식 "가능 시간대 보기" 삭제, 병합 셀 대응(`fillMergedCells`) 추가.
- UI 2차 개편 15건 일괄 반영(위 "UI 2차 개편" 섹션 참고): 교사별 임의배정 인원 사전 설정(비교 시작 전 노출 + 스케줄러 파일 미리 읽기), 회원별 보기 컬럼 재구성(상품 단순화 "자주표"/"전과목/단과", 레벨 "Lv" 제거 및 색상 제거, 예비초/알파벳→화면엔 "알파벳" 표시), 레벨 필터 고정 목록화, 매칭 교사별 색상 구분 박스, 교사별 보기 레벨 "Lv" 제거 + 간략 가능시간 텍스트, 슬롯(요일·시간 칸)별 레벨 게이트(빈 칸=레벨무관), 임의배정 결과에 실제 "배정시간" 자동 산출 + "그 외 가능시간" 모달(배정된 시간 제외하고 표시), "미배정 회원"→"추가 배정 가능 회원"(완전 매칭불가 회원 제외, 전체 매칭시간 나열).
- UI 3차 개편 5건 일괄 반영(위 "UI 3차 개편" 섹션 참고): "매칭 가능 교사 및 시간"을 슬롯 단위→교사(탭) 단위 박스로 재구성(교사 하나당 박스 하나에 모든 가능 시간 나열), 박스/슬롯 모두 인원 적은 순 정렬, 추가 배정 가능 회원의 시간 3개 이상 시 첫 시간만 보이고 펼치기 버튼으로 전체 표시, 추가 배정 가능 회원의 매칭 박스를 클릭하면 캘린더에서 시간을 선택해 임의 배정 결과에 즉시 수동 추가(교사별 인원 캡 무시) + 수동 추가된 행은 굵은 빨간 테두리와 배지로 강조.
- UI 4차 개편 2건 일괄 반영(위 "UI 4차 개편" 섹션 참고): "매칭 가능 교사 및 시간" 박스를 시간 칩 줄바꿈 방식으로 컴팩트화(고정폭 260px, 교사명 왼쪽 중앙 고정)해서 회원별 보기 한 화면에 가능일정·불가능일정·특이사항·형제까지 함께 보이도록 개선, 자유 텍스트 추가 지침 대신(사용자가 직접 선택) 교사별 레벨 제한 체크박스 카드 신설 — 체크한 레벨만 그 교사와 매칭되도록 `matchAll()`에 게이트 추가 + 체크 즉시 회원별/교사별/임의배정 화면 전체 재반영.
- UI 5차 개편 6건 일괄 반영(위 "UI 5차 개편" 섹션 참고): 개별 스케줄러 탭의 요일/시간/레벨/상품 컬럼 매핑을 실제 파일 기준(G=요일/H=시간/I=레벨/M=상품)으로 수정해 "레벨무관" 과다 노출 버그 해결 + 상품 정보 모달에 보조 표시, 레벨 필터 "베문법"→"문법" 이름 변경, "매칭 가능 교사 및 시간" 박스를 다시 세로 나열(교사명 왼쪽 중앙·시간 세로)로 변경 + 폭 축소(260px→210px), 추가 배정 가능 회원의 "외 N개" 펼치기도 세로 나열 적용, 임의 배정 결과를 레벨 순으로 묶어서(구분 행 삽입) 표시.
- UI 6차 개편 9건 일괄 반영(위 "UI 6차 개편" 섹션 참고): 개별 스케줄러 컬럼 매핑 재수정(H=요일/I=시간/G=레벨·반이름 — UI 5차 매핑도 틀렸던 것으로 재확인), 상품 3분류(전과목/단과/자주표) 체계 도입 및 상품명(I열) 패턴 기준 `productCategory()` 신설, 자주표(`isJaju`) 판별에 구분(B열)뿐 아니라 상품명 패턴도 함께 반영(상호 보완), 회원별 보기에 상품 필터(전체/전과목/단과/자주표) 추가, 회원별 보기 동점자 정렬에 레벨 오름차순 2차 키 추가, "매칭 가능 교사 및 시간" 시간 4개 이상이면 4개까지 노출 후 펼치기(회원별 보기 전용, 다른 화면은 기존 3개/1개 유지), 교사별 색상을 행마다 초기화하지 않고 전역으로 고정해 같은 교사는 표 전체에서 항상 같은 색으로 보이도록 개선.
- UI 7차 개편 3건 일괄 반영(위 "UI 7차 개편" 섹션 참고): 가능 시간대 캘린더 모달 크기 확대(720px→1100px, 85vh→96vh + 셀 촘촘하게)로 스크롤 없이 한 화면에 보이도록 개선, 캘린더 방 레벨 표시에 "Lv" 접두어 복원(숫자 레벨만, 파닉스/알파벳/문법 등은 그대로), A주/B주 참고용 인원 표시를 가로(·) 대신 세로(각 줄)로 변경.
- UI 8차 개편 4건 일괄 반영(위 "UI 8차 개편" 섹션 참고): 캘린더 모달 셀의 `white-space:nowrap`이 진짜 원인이었던 가로 스크롤 재발 문제를 줄바꿈 허용(`white-space:normal;word-break:keep-all;overflow-wrap:break-word`)으로 재수정, A주/B주가 서로 다른 레벨(반)일 수 있음을 반영해 `slotLevelSetsA`/`slotLevelSetsB`·`slotLevelA`/`slotLevelB` 신설 및 `teacherSlotLabel`이 A/B 각각 다른 레벨을 표시하도록 수정, 요약 카드에서 "분석 교사" 수치 칩 삭제.

## 미해결/확인 필요
- "고승명" 교사부터 가능 시간대가 비어 보이는 문제 — `fillMergedCells`(병합 셀 대응, 위 UI 개편 섹션 참고)를 원인으로 추정하고 수정했으나, **실제 스케줄러 파일로 최종 확인 아직 안 됨**. 사용자가 실제 파일로 재현되던 화면과 비교해서 이번 수정으로 해결됐는지 확인 필요.
- **슬롯별 레벨 게이트(위 UI 2차 개편 섹션) — 컬럼 매핑은 UI 5차에서 수정됨(G=요일/H=시간/I=레벨/M=상품), 그래도 실제 파일 최종 검증 필요** — 사용자가 알려준 컬럼 기준으로 고쳤지만, 실제 개별 스케줄러 파일을 업로드해서 교사 시간표 모달에 레벨/상품이 정확히 표시되는지는 아직 확인 못함. 레벨 표기 형식이 `LEVEL_FILTER_OPTIONS`/`normalizeLevelForMatch`와 다르게 나오면 정규화가 실패해 매칭이 과도하게 좁아지거나 넓어질 수 있으니 실제 파일로 반드시 확인 필요.
- 최종 브라우저 테스트(실제 파일 업로드) 미완료 (genN 포함, 이번 수업주차/배정불가회원/교사별탭/슬롯레벨게이트/임의배정 재구성 변경사항 포함).
- **그룹영어 자주표 A/B주 로직 실제 파일 미검증** — Node 모의 데이터로 단위 테스트만 진행함. 실제 배정 파일/스케줄러 파일로 브라우저에서 반드시 재확인 필요. 특히:
  - `AB_WEEK_ANCHOR`(2026-08-24=B주) 기준이 실제 운영 방식과 맞는지
  - B열(구분) 실제 값이 정확히 "자주표"라는 문자열을 포함하는지(다른 표기 가능성) — UI 6차에서 상품명(I열) 패턴 기준 판별을 OR로 추가해 안전장치를 마련했으나, 두 기준(B열/상품명) 모두 실제 파일로 재확인 필요
  - 이미 배정된 회원 중 ★배정 시트에 해당 CAS가 없는 경우(졸업/삭제 등) `fullMemberIndex`에서 못 찾아 보수적으로 "매주"로 취급되는데, 실제로 그런 케이스가 얼마나 있는지
- **이번 UI 개편(요약 카드/회원별 보기 재구성/교사별 임의배정 인원/캘린더 모달) 14건도 실제 파일 미검증** — Playwright 헤드리스 브라우저로 가짜(mock) 데이터를 주입해 클릭/렌더링/정렬 등 화면 동작만 확인했고, 실제 배정 파일·스케줄러 파일을 업로드한 상태에서는 아직 확인하지 않음. 특히 모달 캘린더의 요일/시간 그리드가 실제 `TIME_SLOTS`·요일 데이터와 잘 맞는지, 교사별 임의배정 인원 입력이 실제 교사 수만큼 잘 나오는지 확인 필요.
- **UI 3차 개편(교사 단위 박스 통합/인원순 정렬/캘린더 수동 추가) 5건도 실제 파일 미검증** — Playwright로 가짜 교사 2명·회원 1명 시나리오를 만들어 박스 통합/정렬/펼치기/모달 오픈/시간 선택/추가/강조까지 전체 흐름을 확인했으나, 실제 파일 업로드 후 교사 수가 많을 때 박스 정렬·펼치기 UI가 실제로 보기 편한지, 수동 추가가 기존 자동 배정과 자연스럽게 섞이는지는 사용자가 직접 확인 필요.
- **UI 4차 개편(매칭 박스 컴팩트화/교사별 레벨 제한 체크박스) 2건도 실제 파일 미검증** — Playwright 합성 데이터로 컴팩트 레이아웃 렌더링과 레벨 제한 체크박스의 매칭 게이트 동작(체크 시 즉시 매칭에서 제외/포함, 화면 재반영, 초기화)만 확인함. 실제 교사 탭이 많을 때 "③ 교사별 레벨 제한 설정" 카드가 너무 길어지지 않는지, 실제 레벨 표기(예: "80W" vs "80" 등)가 `LEVEL_FILTER_OPTIONS`와 잘 맞는지는 사용자가 실제 파일로 확인 필요. 추가 지침 텍스트박스는 이제 매칭에 반영되지 않는 참고용 메모임을 사용자에게 안내 완료(화면 문구로도 명시함).
- **UI 5차 개편(스케줄러 컬럼 매핑/레벨필터 이름변경/매칭박스 세로나열/임의배정 레벨그룹핑) 6건도 실제 파일 미검증** — 컬럼 매핑(G/H/I/M열)은 UI 6차에서 재수정됨(아래 참고). 그 외 항목(세로 나열 레이아웃, 레벨별 구분 행)도 Playwright 합성 데이터로만 확인했고, 실제 교사·회원 수가 많을 때 세로 나열이 박스를 지나치게 길게 만들지 않는지, 레벨 구분 행이 실제 다양한 레벨 표기와 잘 맞아떨어지는지 사용자가 실제 파일로 확인 필요.
- **UI 6차 개편(스케줄러 컬럼 재수정/상품 3분류/상품 필터/정렬·색상·접기) 9건 모두 실제 파일 미검증 — 특히 컬럼 매핑은 이번이 두 번째 수정** — 개별 스케줄러 요일/시간/레벨 컬럼을 G/H/I → H/I/G로 다시 수정했지만, 실제 파일 없이는 이게 최종 정답인지 확신할 수 없음. 다음에 실제 스케줄러 파일을 올렸을 때도 여전히 반영이 안 된다면, 컬럼 인덱스 문제가 아니라 다른 원인(탭 이름 패턴이 `parseTabName`과 안 맞아 탭 자체가 스킵되는 경우, 데이터 시작 행이 실제로는 11행(index 10)이 아닌 경우, 병합 셀 처리 등)을 의심해봐야 함. 상품 3분류(`productCategory`)도 사용자가 알려준 3가지 패턴 외의 실제 상품명 변형이 있을 수 있으니 실제 파일로 반드시 확인 필요.
- **UI 7차 개편(캘린더 모달 확대/레벨 Lv 표기/A·B주 세로 표시) 3건도 실제 화면 미확인** — Playwright로 계산된 CSS 값(max-width/max-height)과 함수 반환 HTML만 확인했고, 실제 사용자 모니터에서 캘린더가 정말 스크롤 없이 한 화면에 들어오는지, 확대된 모달이 오히려 화면을 벗어나지 않는지는 사용자가 직접 확인 필요. → **UI 8차에서 실제 원인(셀 텍스트 nowrap)을 찾아 재수정함.**
- **UI 8차 개편(가로 스크롤 재수정/A·B주 레벨 개별 표시/요약 카드 정리) 4건도 실제 파일 미검증** — 이번엔 480px~1280px 여러 뷰포트 + 60칸 전체 데이터 + 긴 상품명으로 가로 스크롤이 정말 안 생기는지 Playwright로 직접 재현·검증했고, `parseSchedulerFile`이 같은 칸에서 A/B를 서로 다른 레벨로 정확히 분리하는지도 vm 테스트로 확인했지만, 여전히 합성 데이터 기준이라 **실제 스케줄러 파일(특히 실제 상품명 길이, 실제 A/B 레벨이 다른 케이스)로 사용자가 최종 확인 필요**. 이번에도 안 고쳐졌다고 하면 다음엔 사용자 화면의 브라우저 창 폭/배율이나 실제 파일의 특이한 값(예: 상품명이 테스트보다 훨씬 긴 경우, 폰트 설정 차이)을 의심해봐야 함.
- 다음 작업: 기수별(반별?) 파일 생성 기능 추가 예정.
